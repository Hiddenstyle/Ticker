var assert = chai.assert,
    expect = chai.expect,
    should = chai.should();

var tickerStub = chai.stubb();

describe('#Ticker.startTicker', function() {
  it("should not start ticking if it is a lready ticking.", function() {
    var doTick = true;



  });
}) ;
